Git Review Notes:

User: esles
email: lesteres@utas.edu.au

Basic How To's:

$ git config --global user.name "name"

$ git config -h (config commands)
$ git config -help (git manual)

$ mkdir     (make directory)
$ cd        (change directory)
            (now in current directory)

$ git init    (create repository (hidden storage area))
$ ls -a       (list -a show all)
$ pwd         (present working directory)
$ rm          (remove)

$ git status

(if using in-built nano text editor say)
$ nano test.txt   (to write)
$ cat test.txt    (to read)


$ git add test.txt  (sends test.txt to staging area)

$ git commit        (makes staged items permanently stored in local repository)
(or)
$ git commit -m "log message indicating changes"

$ git diff
(or)
$ git diff --staged
(or)
$ git show

$ git log

$ git diff HEAD~2 text.txt
 (HEAD is most recent commit; HEAD~1 = penultimate, etc.)
 (can alse use first few digits of commit ID)

 $ git checkout HEAD test.txt   (goes back to previous version)

 nano .gitignore
 (files listed in gitignore are ignored by git (duh))
